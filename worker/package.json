{
  "name": "nexaro-ai-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/ai.js && node --check src/auth.js && node --check src/agent.js && node --check src/documents.js && node --check src/r2.js && node --check src/utils.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
